import { Newtype } from '.';
import { Prism } from 'monocle-ts';
export interface Integer extends Newtype<{
    readonly Integer: unique symbol;
}, number> {
}
export declare const isInteger: (n: number) => boolean;
export declare const prismInteger: Prism<number, Integer>;
