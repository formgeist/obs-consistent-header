import { At, Lens } from '../index';
import * as SM from 'fp-ts/lib/StrMap';
export function atStrMap() {
    return new At(function (at) { return new Lens(function (s) { return SM.lookup(at, s); }, function (a) { return function (s) { return a.fold(SM.remove(at, s), function (x) { return SM.insert(at, x, s); }); }; }); });
}
