import { BBox, BBoxCalculator } from './bbox_calculator';
export declare class CanvasTextBBoxCalculator implements BBoxCalculator {
    private attachedRoot;
    private offscreenCanvas;
    private context;
    constructor(rootElement?: HTMLElement);
    compute(text: string, padding: number, fontSize?: number, fontFamily?: string, lineHeight?: number, fontWeight?: number): BBox;
    destroy(): void;
}
