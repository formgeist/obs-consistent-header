export interface BBox {
    width: number;
    height: number;
}
export declare const DEFAULT_EMPTY_BBOX: {
    width: number;
    height: number;
};
export interface BBoxCalculator {
    compute(text: string, padding: number, fontSize?: number, fontFamily?: string): BBox;
    destroy(): void;
}
