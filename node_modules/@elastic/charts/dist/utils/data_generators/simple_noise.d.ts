export declare type RandomNumberGenerator = () => number;
export declare class Simple1DNoise {
    private maxVertices;
    private maxVerticesMask;
    private amplitude;
    private scale;
    private getRandomNumber;
    constructor(randomNumberGenerator?: RandomNumberGenerator, maxVertices?: number, amplitude?: number, scale?: number);
    getValue(x: number): number;
    private lerp;
}
