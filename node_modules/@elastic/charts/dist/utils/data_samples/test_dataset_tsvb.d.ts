export declare const TSVB_DATASET: {
    id: string;
    series: {
        id: string;
        label: string;
        color: string;
        data: (number | null)[][];
        stack: boolean;
        lines: {
            show: boolean;
            fill: number;
            lineWidth: number;
            steps: boolean;
        };
        points: {
            show: boolean;
            radius: number;
            lineWidth: number;
        };
        bars: {
            show: boolean;
            fill: number;
            lineWidth: number;
        };
    }[];
};
