import { TickFormatter } from '../../chart_types/xy_chart/utils/specs';
export declare function timeFormatter(format: string): TickFormatter;
export declare function niceTimeFormatter(domain: [number, number]): TickFormatter;
export declare function niceTimeFormatByDay(days: number): "YYYY-MM-DD" | "MMMM DD" | "MM-DD HH:mm" | "HH:mm:ss";
