import { Spec } from '../specs';
export interface DispatchProps {
    upsertSpec: (spec: Spec) => void;
    removeSpec: (id: string) => void;
}
export declare function specComponentFactory<U extends Spec, D extends keyof U>(defaultProps: Pick<U, D | 'chartType' | 'specType'>): {
    (props: U & DispatchProps): null;
    defaultProps: Pick<U, "specType" | "chartType" | D>;
};
export declare function getConnect(): import("react-redux").InferableComponentEnhancerWithProps<DispatchProps, {}>;
