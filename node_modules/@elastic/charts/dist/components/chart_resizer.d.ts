import React from 'react';
import { Dimensions } from '../utils/dimensions';
interface ResizerStateProps {
    resizeDebounce: number;
}
interface ResizerDispatchProps {
    updateParentDimensions(dimension: Dimensions): void;
}
declare type ResizerProps = ResizerStateProps & ResizerDispatchProps;
declare class Resizer extends React.Component<ResizerProps> {
    private initialResizeComplete;
    private containerRef;
    private ro;
    private animationFrameID;
    private onResizeDebounced;
    constructor(props: ResizerProps);
    componentDidMount(): void;
    componentWillUnmount(): void;
    onResize: (entries: ResizeObserverEntry[]) => void;
    render(): JSX.Element;
    private handleResize;
}
export declare const ChartResizer: import("react-redux").ConnectedComponent<typeof Resizer, Pick<ResizerProps, never>>;
export {};
