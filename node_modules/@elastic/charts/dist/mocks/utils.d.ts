import seedrandom from 'seedrandom';
import { DataGenerator } from '../../src';
/**
 * Forces object to be partial type for mocking tests
 *
 * SHOULD NOT BE USED OUTSIDE OF TESTS!!!
 *
 * @param obj partial object type
 */
export declare const forcedType: <T extends object>(obj: Partial<T>) => T;
export declare const getRandomNumber: seedrandom.prng;
export declare class SeededDataGenerator extends DataGenerator {
    constructor(frequency?: number);
}
