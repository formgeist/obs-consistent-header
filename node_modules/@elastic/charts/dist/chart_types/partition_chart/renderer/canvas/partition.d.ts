import React from 'react';
import { onChartRendered } from '../../../../state/actions/chart';
import { Dimensions } from '../../../../utils/dimensions';
import { ShapeViewModel } from '../../layout/types/viewmodel_types';
interface ReactiveChartStateProps {
    initialized: boolean;
    geometries: ShapeViewModel;
    chartContainerDimensions: Dimensions;
}
interface ReactiveChartDispatchProps {
    onChartRendered: typeof onChartRendered;
}
declare type PartitionProps = ReactiveChartStateProps & ReactiveChartDispatchProps;
declare class PartitionComponent extends React.Component<PartitionProps> {
    static displayName: string;
    private readonly canvasRef;
    private ctx;
    private readonly devicePixelRatio;
    constructor(props: Readonly<PartitionProps>);
    private drawCanvas;
    private tryCanvasContext;
    componentDidUpdate(): void;
    componentDidMount(): void;
    render(): JSX.Element | null;
}
export declare const Partition: import("react-redux").ConnectedComponent<typeof PartitionComponent, Pick<PartitionProps, never>>;
export {};
