import { Font, TextMeasure } from '../types/types';
import { Pixels } from '../types/geometry_types';
export declare function cssFontShorthand({ fontStyle, fontVariant, fontWeight, fontFamily }: Font, fontSize: Pixels): string;
export declare function measureText(ctx: CanvasRenderingContext2D): TextMeasure;
