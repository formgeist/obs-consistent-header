import { ArrayEntry, HierarchyOfArrays } from './group_by_rollup';
import { Part } from '../types/types';
interface LayoutElement {
    nodes: HierarchyOfArrays;
    dependentSize: number;
    sectionSizes: number[];
    sectionOffsets: number[];
}
export declare function leastSquarishAspectRatio({ sectionSizes, dependentSize }: LayoutElement): number;
export declare function treemap(nodes: HierarchyOfArrays, areaAccessor: (e: ArrayEntry) => number, paddingAccessor: (e: ArrayEntry) => number, { x0, y0, width, height }: {
    x0: number;
    y0: number;
    width: number;
    height: number;
}): Array<Part>;
export {};
