import React from 'react';
import { Rotation } from '../../utils/specs';
import { Theme } from '../../../../utils/themes/theme';
import { Dimensions } from '../../../../utils/dimensions';
import { BarGeometry } from '../../../../utils/geometry';
interface BarValuesProps {
    theme: Theme;
    chartDimensions: Dimensions;
    chartRotation: Rotation;
    debug: boolean;
    bars: BarGeometry[];
}
export declare class BarValuesComponent extends React.Component<BarValuesProps> {
    shouldComponentUpdate(nextProps: BarValuesProps): boolean;
    render(): JSX.Element | undefined;
    private renderBarValues;
}
export declare const BarValues: import("react-redux").ConnectedComponent<typeof BarValuesComponent, Pick<BarValuesProps, never>>;
export {};
