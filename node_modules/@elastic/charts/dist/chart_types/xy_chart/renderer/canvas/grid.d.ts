import React from 'react';
import { AxisLinePosition } from '../../utils/axis_utils';
import { Theme } from '../../../../utils/themes/theme';
import { Dimensions } from '../../../../utils/dimensions';
import { AxisId } from '../../../../utils/ids';
import { AxisSpec } from '../../../../chart_types/xy_chart/utils/specs';
interface GridProps {
    chartTheme: Theme;
    axesGridLinesPositions: Map<AxisId, AxisLinePosition[]>;
    axesSpecs: AxisSpec[];
    chartDimensions: Dimensions;
}
declare class GridComponent extends React.Component<GridProps> {
    shouldComponentUpdate(nextProps: GridProps): boolean;
    render(): JSX.Element[];
    private renderGridLine;
}
export declare const Grid: import("react-redux").ConnectedComponent<typeof GridComponent, Pick<GridProps, never>>;
export {};
