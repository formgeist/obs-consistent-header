import React from 'react';
import { TooltipType } from '../../utils/interactions';
import { Dimensions } from '../../../../utils/dimensions';
import { Theme } from '../../../../utils/themes/theme';
import { Rotation } from '../../../../chart_types/xy_chart/utils/specs';
interface CrosshairProps {
    theme: Theme;
    chartRotation: Rotation;
    cursorBandPosition?: Dimensions;
    cursorLinePosition?: Dimensions;
    tooltipType: TooltipType;
}
declare class CrosshairComponent extends React.Component<CrosshairProps> {
    static displayName: string;
    render(): JSX.Element;
    renderBand(): JSX.Element | null;
    renderLine(): JSX.Element | null;
}
export declare const Crosshair: import("react-redux").ConnectedComponent<typeof CrosshairComponent, Pick<CrosshairProps, never>>;
export {};
