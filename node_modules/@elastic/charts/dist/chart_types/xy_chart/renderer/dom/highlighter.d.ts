import React from 'react';
import { IndexedGeometry } from '../../../../utils/geometry';
import { Dimensions } from '../../../../utils/dimensions';
import { Rotation } from '../../../../chart_types/xy_chart/utils/specs';
import { Transform } from '../../state/utils';
interface HighlighterProps {
    initialized: boolean;
    chartId: string;
    highlightedGeometries: IndexedGeometry[];
    chartTransform: Transform;
    chartDimensions: Dimensions;
    chartRotation: Rotation;
}
declare class HighlighterComponent extends React.Component<HighlighterProps> {
    static displayName: string;
    render(): JSX.Element;
}
export declare const Highlighter: import("react-redux").ConnectedComponent<typeof HighlighterComponent, Pick<HighlighterProps, never>>;
export {};
