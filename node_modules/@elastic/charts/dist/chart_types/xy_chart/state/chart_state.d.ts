import { RefObject } from 'react';
import { InternalChartState, GlobalChartState, BackwardRef } from '../../../state/chart_state';
import { TooltipLegendValue } from '../tooltip/tooltip';
import { Stage } from 'react-konva';
export declare class XYAxisChartState implements InternalChartState {
    chartType: "xy_axis";
    legendId: string;
    isBrushAvailable(globalState: GlobalChartState): boolean;
    isBrushing(globalState: GlobalChartState): boolean;
    isChartEmpty(globalState: GlobalChartState): boolean;
    getLegendItems(globalState: GlobalChartState): Map<string, import("../legend/legend").LegendItem>;
    getLegendItemsValues(globalState: GlobalChartState): Map<string, TooltipLegendValue>;
    chartRenderer(containerRef: BackwardRef, forwardStageRef: RefObject<Stage>): JSX.Element;
    getPointerCursor(globalState: GlobalChartState): string;
}
