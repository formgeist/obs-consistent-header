import { TooltipValue } from '../utils/interactions';
import { AxisSpec, BasicSeriesSpec } from '../utils/specs';
import { IndexedGeometry } from '../../../utils/geometry';
export interface TooltipLegendValue {
    y0: any;
    y1: any;
}
export declare const Y0_ACCESSOR_POSTFIX = " - lower";
export declare const Y1_ACCESSOR_POSTFIX = " - upper";
export declare function getSeriesTooltipValues(tooltipValues: TooltipValue[], defaultValue?: string): Map<string, TooltipLegendValue>;
export declare function formatTooltip({ color, value: { x, y, accessor }, seriesIdentifier }: IndexedGeometry, spec: BasicSeriesSpec, isXValue: boolean, isHighlighted: boolean, hasSingleSeries: boolean, axisSpec?: AxisSpec): TooltipValue;
